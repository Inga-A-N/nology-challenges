

const createMessage = async (messageData) => {
  const newMessage = {...messageData, createAt: new Date()}
  const response = await fetch (), {method: "post", 
headers: {}}
}

export default message-service